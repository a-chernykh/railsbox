description "{{ app_name }} delayed_job"

start on [2]
stop on runlevel [016]

env PATH=$PATH:/usr/local/bin:/usr/bin

respawn

setuid {{ user_name }}
setgid {{ group_name }}

chdir {{ app_path }}

script
  bundle exec {{ delayed_job_command }}
  sleep 5
end script
